# Changelog

## 0.0.2 - 2026-04-08

- Added namespace-owned docs so `%` can describe `[namespace]` entries directly.
- Updated CLI task listing and namespace help to use namespace docs instead of leaking the first task doc.
- Cleaned up outdated Windows probe coverage by standardizing on `@has(...)`.
- Refined the project `Onlyfile` with `nextest`-first testing and clearer `dev` / `rel` namespaces.
- Aligned README and usage docs with the currently implemented feature set.
